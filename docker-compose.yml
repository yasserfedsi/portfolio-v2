services:
  frontend:
    container_name: next-app
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "${EXTERNAL_PORT}:${INTERNAL_PORT}"
    restart: unless-stopped
