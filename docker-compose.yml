services:
  next:
    container_name: next-app
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=3000